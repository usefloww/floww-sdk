{
  "type": "module",
  "scripts": {
    "build": "esbuild --bundle src/server.ts --outdir=dist --target=esnext --platform=node --external:floww --external:fastify",
    "dev": "node --watch dist/server.js"
  },
  "dependencies": {
    "floww": "file:../../sdk",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "esbuild": "0.25.10",
    "@types/node": "^22.0.0"
  }
}
